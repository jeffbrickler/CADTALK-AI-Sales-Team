# v2.17.0 Self-Improving Loop Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Give the plugin a self-improving loop — a `ct-retro` skill that harvests session corrections into a shipped `LEARNINGS.md`, promotes recurring ones to TODOS, and catches the plugin's own registration drift; plus an Approach C CRM reconciliation pass on `ct-sweep` that grades deals against the create contract and stages fixes for approval.

**Architecture:** `ct-retro` is a maintenance skill run at end-of-session or on demand. It writes to a plugin-owned `LEARNINGS.md` (travels with fresh installs) and to `TODOS.md`; the plugin `CLAUDE.md` gains one line telling skills to apply relevant learnings before running. The registration check reuses `scripts/validate-plugin.py` (already warns on missing routing rows) and adds a ct-help coverage grep. Approach C extends `ct-sweep`'s existing deterministic pre-pass: it grades each open deal against `scripts/create-contract.json` required fields, auto-fills what context knows, and stages the rest as `reconcile-fill` items into the same `/ct-inbox` review queue — writes still flow through the sales-crm contract on approval. No unattended writes; the sweep runs on schedule or explicit invocation only (anti-nag).

**Tech Stack:** Markdown skill files; existing `scripts/create-contract.json`, `scripts/validate_hygiene.py`, `scripts/pipedrive_read.py`; `scripts/validate-plugin.py` gate.

**Spec:** `docs/superpowers/specs/2026-07-16-self-contained-plugin-design.md` (v2.17 section).

**Content/skills plan — not app code.** Per-task gate = grep assertions + `python scripts/validate-plugin.py` (0 failures, 0 warnings).

---

## File Structure

| File | Responsibility | Task |
|---|---|---|
| `LEARNINGS.md` | CREATE — seed the shipped learnings log with its format header | 1 |
| `skills/ct-retro/SKILL.md` | CREATE — harvest → LEARNINGS/TODOS, registration check | 2 |
| `skills/ct-sweep/SKILL.md` | MODIFY — add Approach C reconciliation pass | 3 |
| `CLAUDE.md` (root) | MODIFY — ct-retro routing row + LEARNINGS-consumption line | 4 |
| `skills/ct-help/SKILL.md` | MODIFY — ct-retro skill-map line + detail block | 4 |
| `.claude-plugin/plugin.json`, `CHANGELOG.md` | MODIFY — bump 2.17.0 + changelog | 5 |

---

### Task 1: Seed `LEARNINGS.md`

The shipped learnings log. Starts empty (just the format header) so fresh installs have the file and the format is self-documenting.

**Files:**
- Create: `LEARNINGS.md`

- [ ] **Step 1: Write the file**

Create `LEARNINGS.md` at the repo root with exactly:

```markdown
# CADTALK Plugin — Learnings Log

Session corrections harvested by `/ct-retro`. This file **ships with the plugin** —
learnings here improve the skills for every rep on the next install. Rep-personal or
deal-specific notes do NOT belong here; those go to the workspace `MEMORY.md`.

Skills check this file before running: if there are entries tagged for the skill
about to run, apply them. When a learning is folded into a skill's own SKILL.md in a
release, `/ct-retro` marks it `folded` and it can be pruned.

## Format

Each learning is one entry:

```
### [skill-tag] short title — YYYY-MM-DD

**What:** the correction or miss observed in a session.
**Why:** the root cause — why the skill did the wrong thing.
**How to apply:** the concrete behavior change the skill should make next time.
**Status:** open | folded (folded = now in the skill's SKILL.md; safe to prune)
```

The `skill-tag` is the target skill without the slash (e.g. `ct-prep`, `ct-crm`,
`sales-crm`). Use `plugin` for cross-cutting learnings.

---

## Learnings

_(none yet — /ct-retro appends here)_
```

- [ ] **Step 2: Verify**

Run: `grep -E '^# CADTALK Plugin — Learnings Log' LEARNINGS.md` → prints.
Run: `grep -E 'How to apply:|Status:|skill-tag' LEARNINGS.md` → all three present.
Run: `python scripts/validate-plugin.py` → 0 failures.

- [ ] **Step 3: Commit**

```bash
git add LEARNINGS.md
git commit -m "feat: seed LEARNINGS.md — shipped self-improvement log (v2.17)"
```

---

### Task 2: `ct-retro` skill

**Files:**
- Create: `skills/ct-retro/SKILL.md`

- [ ] **Step 1: Write the skill**

Create `skills/ct-retro/SKILL.md` with exactly:

````markdown
---
name: ct-retro
description: End-of-session retro — harvest corrections into the plugin LEARNINGS.md, promote recurring ones to TODOS, and check the plugin for registration drift. Use for 'run a retro', 'what did we learn', 'capture that correction', 'check the plugin is consistent'.
---

# CADTALK Retro — the self-improvement loop

Invoked as `/ct-retro`. Run it at the end of a working session, or any time a
correction is worth capturing. Three passes: harvest, promote, check.

## Pass 1 — Harvest corrections → `LEARNINGS.md`

Scan the session for moments the plugin did the wrong thing and the rep corrected it:
- edits the rep made to a skill's output,
- drafts the rep rejected,
- wrong grounding (a brain-index doc ID that 404'd, a stale claim),
- tool-name drift (a Pipedrive tool that no longer exists under that name).

For each, decide where it belongs:
- **Ships with the plugin** (the skill should behave differently for everyone) →
  append an entry to the plugin `LEARNINGS.md` in its format, tagged with the target
  skill (`ct-prep`, `ct-crm`, `sales-crm`, or `plugin` for cross-cutting).
- **Rep-personal or deal-specific** (only true for this rep/deal) → write it to the
  workspace `MEMORY.md`, not `LEARNINGS.md`.
- **Ambiguous** → ask the rep which it is before writing.

Never invent a learning. If the session had no real corrections, say so and write
nothing.

## Pass 2 — Promote recurring learnings → `TODOS.md`

Read `LEARNINGS.md`. If the same `skill-tag` has **two or more open entries** pointing
at the same root cause, that is a signal the skill itself should change. Add a
`TODOS.md` item (in the existing TODOS format: What / Why / Context / Depends on)
proposing the skill fix, and reference the learning entries. Do not fold the change
yourself — that happens in a release.

## Pass 3 — Registration + grounding check

Catch the plugin's own drift (the automated version of the 3-file registration
checklist):

1. Run `python scripts/validate-plugin.py`. It warns on any skill directory missing a
   routing row in the root `CLAUDE.md`. Report every warning.
2. For each `skills/ct-*/` directory, grep `skills/ct-help/SKILL.md` for a matching
   `### ct-<name>` detail block. Report any skill with no ct-help detail block.
3. For each brain-index doc ID flagged as 404 during Pass 1, list it so the next
   release can fix `references/brain-index.md`.

Output a short drift report: routing gaps, ct-help gaps, stale doc IDs. If everything
is registered and grounded, say "no drift."

---

## Output

A single retro summary: N learnings written (with where each went), any TODOS
promoted, and the drift report. Keep it short — this is a maintenance pass, not a
narrative.

## Common mistakes

- Writing deal-specific facts into `LEARNINGS.md` — those go to workspace `MEMORY.md`.
- Inventing learnings to fill the log — only capture real corrections.
- Folding a skill change directly — `/ct-retro` proposes (LEARNINGS/TODOS); releases fold.
````

- [ ] **Step 2: Verify**

Run: `grep -E '^name: ct-retro$' skills/ct-retro/SKILL.md` → prints.
Run: `grep -Ec 'Pass 1|Pass 2|Pass 3' skills/ct-retro/SKILL.md` → at least `3`.
Run: `grep -E 'LEARNINGS.md|validate-plugin.py|MEMORY.md' skills/ct-retro/SKILL.md` → all three present.
Run: `python scripts/validate-plugin.py` → 0 failures (a warning that ct-retro lacks a routing row is expected until Task 4).

- [ ] **Step 3: Commit**

```bash
git add skills/ct-retro/SKILL.md
git commit -m "feat: add ct-retro skill — harvest/promote/registration-check (v2.17)"
```

---

### Task 3: Approach C reconciliation pass on `ct-sweep`

Extend the existing sweep with a create-contract completeness grade. This reuses the
sweep's deterministic pre-pass and its `/ct-inbox` queue — no new queue mechanism.

**Files:**
- Modify: `skills/ct-sweep/SKILL.md`

- [ ] **Step 1: Read the file to find the anchors**

Read `skills/ct-sweep/SKILL.md`. Note the two anchor points:
- "## Step 3: Deterministic pre-pass" (where hygiene candidates are built)
- "## Step 5: Write the queue" (where the sectioned MD is written, sections ①–④)

- [ ] **Step 2: Add the reconciliation sub-pass to Step 3**

At the END of the "## Step 3: Deterministic pre-pass" section (immediately before the
"## Step 4" heading), insert:

```markdown

### Step 3b: Reconciliation grade (Approach C)

Grade each open deal against the CREATE contract — the required fields every deal
should carry — using `scripts/create-contract.json` (`records.deal.required`, plus
the `conditional` fields when their `when` condition holds for the deal's stage/
pipeline). This catches records that skipped the Guided Create Flow (manual web
creates, older deals) — the retro-clean half of CRM enforcement.

For each open deal:
- Compare its populated fields to the contract's required list (map logical names to
  values via the snapshot; never hand-build hash keys).
- A missing required field → a candidate `reconcile-fill` item. If conversation or
  snapshot context already knows the value, pre-fill it in the item; otherwise mark it
  ask-the-rep.
- Skip `excluded_at_create` fields — those are not expected at create time.

These candidates feed the queue as a new section ⑤ (below). Writes still happen only
on approval in `/ct-inbox`, through the sales-crm contract.
```

- [ ] **Step 3: Add section ⑤ to Step 5**

In "## Step 5: Write the queue", find where the sectioned MD sections are listed
(①Hygiene ②Commit integrity ③Stuck & dark ④Today). Add a fifth section to that list
so the written brief includes:

```markdown
   - **⑤ Reconcile** — deals missing CREATE-contract required fields (`reconcile-fill`
     items from Step 3b). Pre-filled where context knew the value; ask-the-rep
     otherwise. Approving writes the field through the sales-crm contract.
```

Match the exact list formatting already used for sections ①–④ in that step (same
indentation and bullet style — read the surrounding lines first).

- [ ] **Step 4: Verify**

Run: `grep -E 'Step 3b: Reconciliation grade' skills/ct-sweep/SKILL.md` → prints.
Run: `grep -E 'create-contract.json|reconcile-fill' skills/ct-sweep/SKILL.md` → both present.
Run: `grep -c 'reconcile-fill' skills/ct-sweep/SKILL.md` → at least `2` (Step 3b + section ⑤).
Run: `python scripts/validate-plugin.py` → 0 failures.

- [ ] **Step 5: Commit**

```bash
git add skills/ct-sweep/SKILL.md
git commit -m "feat: ct-sweep Approach C — CRM reconciliation grade vs create-contract (v2.17)"
```

---

### Task 4: Registration + LEARNINGS consumption

**Files:**
- Modify: `CLAUDE.md` (root)
- Modify: `skills/ct-help/SKILL.md`

- [ ] **Step 1: Add the ct-retro routing row**

In the root `CLAUDE.md` "Skill Routing" table, find the `/ct-contract` row (added in
v2.16). Insert this row immediately **below** it:

```
| End-of-session retro, capture a correction, check the plugin for drift | `/ct-retro` |
```

- [ ] **Step 2: Add the LEARNINGS consumption rule**

In the root `CLAUDE.md`, find the "## Voice — single standard" section heading. Insert
this new section immediately **before** it:

```markdown
## Learnings — apply before running

Before running a `ct-*` skill, check the plugin `LEARNINGS.md`. If it has any open
entries tagged for that skill (or tagged `plugin`), apply them — they are corrections
harvested from real sessions by `/ct-retro` that have not yet been folded into the
skill's own file. This is the plugin's self-improvement loop: learnings ride in
`LEARNINGS.md` until a release folds them in.

```

- [ ] **Step 3: Add ct-retro to ct-help**

In `skills/ct-help/SKILL.md` Full Skill Map, in the `ONBOARDING & TRAINING` block
(after the `/ct-setup` line), add:

```
/ct-retro                       End-of-session retro — capture learnings, check drift
```

Then insert this detail block immediately **before** the `### ct-deal` detail block:

````markdown
### ct-retro

```
/ct-retro
----------
WHAT IT DOES:    The self-improvement loop. Harvests corrections from the session into
                 the plugin LEARNINGS.md, promotes recurring ones to TODOS, and checks
                 the plugin for registration drift (missing routing rows or ct-help
                 blocks) and stale brain-index doc IDs.

WHEN TO RUN IT:  At the end of a working session, or any time a correction is worth
                 keeping so the skill does better next time.

HOW TO USE IT:   /ct-retro

WHAT YOU'LL GET: A retro summary — learnings written (and where each went: LEARNINGS.md
                 for plugin-wide, MEMORY.md for rep/deal-specific), any TODOS promoted,
                 and a drift report.

COMMON MISTAKES:
• Expecting it to fix skills directly — it proposes (LEARNINGS/TODOS); releases fold.
• Putting deal-specific facts in LEARNINGS.md — those go to the workspace MEMORY.md.
```

---
````

- [ ] **Step 4: Verify**

Run: `grep '/ct-retro' CLAUDE.md` → the routing row prints.
Run: `grep -E '^## Learnings — apply before running' CLAUDE.md` → prints.
Run: `grep -c '### ct-retro' skills/ct-help/SKILL.md` → `1`.
Run: `python scripts/validate-plugin.py` → **0 failures, 0 warnings**.

- [ ] **Step 5: Commit**

```bash
git add CLAUDE.md skills/ct-help/SKILL.md
git commit -m "docs: register ct-retro + LEARNINGS consumption rule (v2.17)"
```

---

### Task 5: Version bump + CHANGELOG

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Bump version**

In `.claude-plugin/plugin.json` set `"version"` to `"2.17.0"`.

- [ ] **Step 2: Prepend changelog entry**

In `CHANGELOG.md`, immediately after the `---` that precedes `## v2.16.0 — 2026-07-16`,
insert (keeping the existing v2.16.0 entry below):

```markdown
## v2.17.0 — 2026-07-16

Self-improving loop: the plugin now learns from its own use and heals its own CRM data. Spec: `docs/superpowers/specs/2026-07-16-self-contained-plugin-design.md`.

### Added
- **`/ct-retro`** — end-of-session retro. Harvests session corrections into a shipped `LEARNINGS.md` (plugin-wide) or the workspace `MEMORY.md` (rep/deal-specific), promotes recurring learnings to `TODOS.md`, and runs a registration/grounding drift check (missing routing rows, missing ct-help blocks, stale brain-index IDs).
- **`LEARNINGS.md`** — the shipped learnings log. Skills apply any open entries tagged for them before running; releases fold learnings into the skills and mark them `folded`.
- **`/ct-sweep` Approach C reconciliation** — grades every open deal against the CREATE contract (`scripts/create-contract.json`) and stages `reconcile-fill` items into the `/ct-inbox` queue, pre-filled where context knows the value. Retro-cleans deals that skipped the Guided Create Flow. Writes still flow through the sales-crm contract on approval; runs on schedule or explicit invocation only.

### Changed
- Root `CLAUDE.md` gains a "Learnings — apply before running" rule so every skill consults `LEARNINGS.md` first.

---
```

- [ ] **Step 3: Verify + validate**

Run: `grep '2.17.0' .claude-plugin/plugin.json CHANGELOG.md` → both print.
Run: `python scripts/validate-plugin.py` → 0 failures, 0 warnings.

- [ ] **Step 4: Commit**

```bash
git add .claude-plugin/plugin.json CHANGELOG.md
git commit -m "chore: bump to v2.17.0 + changelog (self-improving loop)"
```

---

## Final verification

- [ ] `python scripts/validate-plugin.py` → 0 failures, 0 warnings.
- [ ] `git log --oneline origin/main..HEAD` shows 5 task commits.
- [ ] `ls skills/ct-retro/SKILL.md LEARNINGS.md` — both exist.
- [ ] `grep -c reconcile-fill skills/ct-sweep/SKILL.md` → ≥ 2.

## Notes for the implementer

- Windows/Git-Bash for verification greps; Edit/Write for file changes.
- ct-retro and LEARNINGS.md are plugin-owned (not vendored) — safe to hand-author.
- `scripts/create-contract.json` already exists; Task 3 only references it, does not change it. Changing that file has a Pipedrive-side sync rule (see its `_comment`) — out of scope here.
- Do not weaken the sweep's no-unattended-writes guarantee: Approach C only *stages* items; approval and the actual write stay in `/ct-inbox` via sales-crm.
